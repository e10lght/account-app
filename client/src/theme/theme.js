import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        background: {
            default: "#cccccc",
        },
        text: { primary: '#333333' },
    },
    secondary: {
        main: "#0e5a88"
    }
})